version: '3.1'

services:
  redmine:
    image: redmine
    restart: always
    ports:
      - 3000:3000
    environment: 
      env_file: /etc/.env

  db:
    image: mysql:8.0
    restart: always
    environment:
      env_file: /etc/.env
